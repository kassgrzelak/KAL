//
// Generated by boilerplateGenerator.py on 24/03/2026.
//

#ifndef KAL_JMPNZ_HANDLERS_H
#define KAL_JMPNZ_HANDLERS_H

static void JMPNZ_RCHandler(VM* vm)
{
	if (REG() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = CONST(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_MCHandler(VM* vm)
{
	if (MEM() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = CONST(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_PCHandler(VM* vm)
{
	if (PTR() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = CONST(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_RRHandler(VM* vm)
{
	if (REG() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = REG(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_RMHandler(VM* vm)
{
	if (REG() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = MEM(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_RPHandler(VM* vm)
{
	if (REG() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = PTR(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_MRHandler(VM* vm)
{
	if (MEM() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = REG(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_MMHandler(VM* vm)
{
	if (MEM() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = MEM(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_MPHandler(VM* vm)
{
	if (MEM() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = PTR(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_PRHandler(VM* vm)
{
	if (PTR() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = REG(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_PMHandler(VM* vm)
{
	if (PTR() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = MEM(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPNZ_PPHandler(VM* vm)
{
	if (PTR() == 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = PTR(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

#endif