//
// Generated by boilerplateGenerator.py on 24/03/2026.
//

#ifndef KAL_JMPZ_HANDLERS_H
#define KAL_JMPZ_HANDLERS_H

static void JMPZ_RCHandler(VM* vm)
{
	if (REG() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = CONST(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_MCHandler(VM* vm)
{
	if (MEM() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = CONST(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_PCHandler(VM* vm)
{
	if (PTR() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = CONST(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_RRHandler(VM* vm)
{
	if (REG() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = REG(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_RMHandler(VM* vm)
{
	if (REG() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = MEM(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_RPHandler(VM* vm)
{
	if (REG() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = PTR(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_MRHandler(VM* vm)
{
	if (MEM() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = REG(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_MMHandler(VM* vm)
{
	if (MEM() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = MEM(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_MPHandler(VM* vm)
{
	if (MEM() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = PTR(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_PRHandler(VM* vm)
{
	if (PTR() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = REG(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_PMHandler(VM* vm)
{
	if (PTR() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = MEM(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

static void JMPZ_PPHandler(VM* vm)
{
	if (PTR() != 0)
    {
        vm->ip += 2;
	    return;
    }

	for (uint8_t skipNum = PTR(); skipNum > 0; --skipNum)
		vm->ip += opcodeLengthTable[*vm->ip];
}

#endif